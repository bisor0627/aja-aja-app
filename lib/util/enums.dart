enum AuthStates {
  unknown,
  authenticated,
  unauthenticated,
}

enum DeviceType {
  unknown,
  ios,
  android,
  web,
}

enum GenderType {
  male,
  female,
}

enum CubitStatus {
  initial,
  loading,
  success,
  failure,
}

enum TriggerStatus {
  init,
  on,
  off,
}
